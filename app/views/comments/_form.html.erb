<div class="text-center">
  <%= form_for ([@blog, @blog.comments.build]) do |form| %>
    <p><%= form.label :body %></p>
    <p><%= form.text_field :body %></p>
    <p><%= form.submit %></p>
  <% end %>
</div>
